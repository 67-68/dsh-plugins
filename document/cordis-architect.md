# cordis-architect 模式经验（继承自 cordis 模式）

#### [overview] 继承 cordis.md 经验

本模式（cordis-architect）的 cordis 插件开发 / preset / 排错经验已全部收敛到 `document/cordis.md`，并以 `cordis-*` skill 全局可见，本文件不再重复。

需要查经验时直接加载对应 `cordis-*` skill（如 `cordis-preset-lookup`、`cordis-dual-face-package-pitfalls`、`cordis-tool-cordis-provider-conflict`），或加载 `mode-experience` 索引 skill 查看完整清单。

本模式独有的内容仅剩架构师人设与工作流（见 persona 及 `architect-workflow` / `architect-delegation` / `orchestration` skill），无 cordis 层面的独有经验增量。
