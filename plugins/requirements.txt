dsh-web-plugin-manager@0.3.10
# 外部插件声明清单：每行一个 source@version（版本锁死，不用 @latest）。
# `#` 开头为注释；空行会被 install.sh 跳过。
# 升级 = 手动改版本号 + 重跑 ./install.sh（pnpm 对已装版本是 no-op，幂等）。
dsh-music-alert@0.1.0
# 临时禁用 dsh-message-edit（2026-09-09）：
# 其 client bundle 以 externals 形式 require("@deepseek-ai/dsh-client-runtime/client")，
# 而 DSH 升级后已删除 dsh-client-runtime 这个 platform seed word（连 0.2.3 最新版也未适配），
# 导致启动时 "client-modules: require(...) missed the module table" 崩溃。
# 上游仓库：https://github.com/Moeblack/dsh-message-edit
# 待上游修复后取消注释并恢复此插件。
# dsh-message-edit@0.2.1

# 归档会话管理器（UI）：侧边栏归档入口，浏览/搜索/恢复/安全删除归档会话。
dsh-chat-manager@1.2.2
# 能力库：Skills & MCP 可视化管理，mcp_load 在当前会话热重载 MCP 工具列表。
dsh-skill-mcp-manager@1.1.1
# @ 文件下拉选择（Codex 风格 @ 引用）：输入 @ 搜工作区文件/目录并引用路径。
# 用 GitHub tarball（v0.7.0）：npm 的 @freespace8/dsh-at-file 停在 0.2.3（发送时内联内容、有大小限制）；
# 0.6.0 起只插 <workspace-reference> 路径标记、agent 自己 read；v0.7.0 适配 DSH 升级：
# @deepseek-ai/dsh-settings@0.1.2-rc.1 移除了 settingsNamespace 具名导出，
# v0.6.0 的 bundle 仍 import 它导致 "does not provide an export named 'settingsNamespace'" 启动崩溃。
# 版本锁死在 URL tag 上（不适用 source@version）。
https://github.com/omdsh-dev/dsh-at-file/archive/refs/tags/v0.7.0.tar.gz
# dsh-read-url 改为本地 fork（packages/dsh-read-url）：上游 npm/GitHub 包带未声明的
# 可选动态 import，过不了 DSH 插件管理器质量门；本地包由 install.sh 拷贝并由
# profile/cordis.patch.yml 挂载，因此不列在外部插件清单里。
# 已移除 @visol-456/dsh-llm-fallback@0.1.2：
# 其 client 面 require("@deepseek-ai/dsh-client-web-react")，而该包是 DSH 官方内部
# React 绑定库、不声明 dsh.client，永远不会进入客户端模块表，导致 `missed the
# module table` 启动失败。官方包（如 settings-general）是把它打包内联，上游
# llm-fallback 却当运行时外部依赖。等上游修复后再恢复。
